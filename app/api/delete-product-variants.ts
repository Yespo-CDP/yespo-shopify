/**
 * Temporary stub for the Yespo product variants delete API.
 *
 * Replace with a real HTTP call once the Yespo endpoint is available.
 */
export const deleteProductVariants = async ({
  apiKey,
  externalVariantIds,
  domain,
  orgId,
}: {
  apiKey: string;
  externalVariantIds: string[];
  domain: string;
  orgId?: number | null;
}): Promise<void> => {
  // FIXME: Implement the actual API call
  void apiKey;
  void externalVariantIds;
  void domain;
  void orgId;
};
